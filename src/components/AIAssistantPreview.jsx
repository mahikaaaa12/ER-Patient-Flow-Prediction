import { Bot, Info, Send, Sparkles, User } from "lucide-react";

const SUGGESTED_QUESTIONS = [
  "Summarize today's patient flow forecast.",
  "Is there a patient surge expected?",
  "How crowded will the ER be tonight?",
];

export default function AIAssistantPreview() {
  return (
    <section id="ai-assistant" className="relative border-t border-border bg-bg">
      <div className="mx-auto max-w-7xl px-4 py-16 sm:px-6 sm:py-20 lg:px-8 lg:py-24">
        <div className="grid grid-cols-1 items-center gap-12 lg:grid-cols-2 lg:gap-16">
          {/* Copy column */}
          <div>
            <span className="inline-flex items-center gap-1.5 rounded-full border border-border bg-surface px-3 py-1.5 text-[13px] font-medium text-amber shadow-soft">
              AI Operations Assistant
            </span>
            <h2 className="mt-5 text-3xl font-semibold leading-tight tracking-tight text-navy sm:text-4xl">
              Ask Your Forecast a Question
            </h2>
            <p className="mt-4 text-[17px] leading-relaxed text-navy-muted">
              Administrators can ask plain-language questions about current and
              upcoming ER conditions and get a clear, contextual answer drawn from
              the platform&apos;s live predictions.
            </p>

            <ul className="mt-8 flex flex-col gap-3">
              {[
                "Plain-language answers about arrivals, wait times and crowding",
                "Always grounded in the underlying model outputs",
                "Available alongside the operations dashboard",
              ].map((point) => (
                <li key={point} className="flex items-start gap-3">
                  <span className="mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-amber-tint text-amber">
                    <Sparkles className="h-3 w-3" strokeWidth={2.5} aria-hidden="true" />
                  </span>
                  <span className="text-[15px] leading-relaxed text-navy-muted">{point}</span>
                </li>
              ))}
            </ul>

            <div className="mt-8 flex items-start gap-3 rounded-xl border border-border bg-surface p-4 shadow-soft">
              <span className="mt-0.5 flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-blue-tint text-blue">
                <Info className="h-4 w-4" strokeWidth={2.25} aria-hidden="true" />
              </span>
              <p className="text-[13px] leading-relaxed text-navy-muted">
                The AI assistant explains predictions generated by the underlying ML
                models. It does not generate the numerical forecasts itself.
              </p>
            </div>
          </div>

          {/* Chat mock column */}
          <div className="relative w-full max-w-md rounded-2xl border border-border bg-surface shadow-lift lg:max-w-lg lg:justify-self-end">
            {/* Header */}
            <div className="flex items-center justify-between border-b border-border px-5 py-4">
              <div className="flex items-center gap-3">
                <span className="flex h-9 w-9 items-center justify-center rounded-lg bg-navy text-white">
                  <Bot className="h-4.5 w-4.5" strokeWidth={2.25} aria-hidden="true" />
                </span>
                <div>
                  <p className="text-[14px] font-semibold text-navy">ERFlow Assistant</p>
                  <p className="text-[12px] text-navy-soft">Grounded in live forecast data</p>
                </div>
              </div>
              <span className="inline-flex items-center gap-1.5 rounded-full bg-green-tint px-2.5 py-1 text-[12px] font-semibold text-green">
                <span className="h-1.5 w-1.5 rounded-full bg-green animate-soft-pulse" />
                Online
              </span>
            </div>

            {/* Conversation */}
            <div className="flex flex-col gap-4 px-5 py-5">
              <div className="flex justify-end gap-2.5">
                <div className="max-w-[80%] rounded-2xl rounded-tr-sm bg-blue px-4 py-2.5">
                  <p className="text-[14px] leading-relaxed text-white">
                    When is the ER expected to be busiest today?
                  </p>
                </div>
                <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-blue-tint text-blue">
                  <User className="h-3.5 w-3.5" strokeWidth={2.25} aria-hidden="true" />
                </span>
              </div>

              <div className="flex justify-start gap-2.5">
                <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-navy text-white">
                  <Bot className="h-3.5 w-3.5" strokeWidth={2.25} aria-hidden="true" />
                </span>
                <div className="max-w-[85%] rounded-2xl rounded-tl-sm border border-border bg-bg px-4 py-2.5">
                  <p className="text-[14px] leading-relaxed text-navy">
                    Patient demand is expected to peak between{" "}
                    <span className="font-semibold">6:00 PM and 9:00 PM</span>. The
                    current forecast indicates approximately{" "}
                    <span className="font-semibold">42 arrivals</span> over the next
                    three hours, with crowding risk increasing to{" "}
                    <span className="font-semibold text-red">High</span>.
                  </p>
                </div>
              </div>
            </div>

            {/* Suggested questions */}
            <div className="border-t border-border px-5 py-4">
              <p className="text-[11px] font-semibold uppercase tracking-wide text-navy-soft">
                Suggested questions
              </p>
              <div className="mt-2.5 flex flex-col gap-2">
                {SUGGESTED_QUESTIONS.map((question) => (
                  <button
                    key={question}
                    type="button"
                    className="w-fit rounded-full border border-border bg-bg px-3 py-1.5 text-left text-[12.5px] font-medium text-navy-muted transition-colors hover:border-blue/40 hover:bg-blue-tint hover:text-blue"
                  >
                    {question}
                  </button>
                ))}
              </div>
            </div>

            {/* Input (visual only — not a functional chatbot) */}
            <div className="flex items-center gap-2 border-t border-border px-5 py-4">
              <div className="flex-1 rounded-lg border border-border bg-bg px-3.5 py-2.5">
                <p className="text-[13px] text-navy-soft">Ask about ER demand&hellip;</p>
              </div>
              <span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-lg bg-blue text-white">
                <Send className="h-4 w-4" strokeWidth={2.25} aria-hidden="true" />
              </span>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
